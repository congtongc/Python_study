# 람다식 : 함수의 형식을 더욱 짧게 쓸 수 있도록 해주는 문법
# map() : 다수의 원소에 대한 함수의 결과를 한 번에 얻을 수 있도록 도와줌
# add = lambda x , y : x + y
# print(add(1,2))

# list1 = [1,2,3,4,5]
# list2 = [6,7,8,9,10]
# my_function = lambda a , b : a * b
# result = map(my_function, list1, list2)
# print(list(result))

# lambda 함수는 메모리 사용이 적음(사용한 후에 메모리에서 삭제) => 재사용 불가(일회용), 훨씬 효율적임
